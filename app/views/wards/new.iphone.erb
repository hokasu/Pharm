<% title "New ward" %>
<% form_for(@ward, 
  :html => { :class => 'panel',
    :selected => 'true',
    :title => 'Make a new\'n'})do |f| %>
  <%= f.error_messages %>

<%= render :partial => 'form', :locals => {:f => f} %>
  <p>
    <%#= f.submit 'Create' %>
    <a class="whiteButton" type="submit" href="#">Create</a>
  </p>
<% end %>
<%= link_to 'Back', wards_path %>
