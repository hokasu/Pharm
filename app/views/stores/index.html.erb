<% title "Stores" %>

<table>
  <tr>
    <th>Ext</th>
    <th>Ward</th>
  </tr>
  <% for store in @stores %>
    <tr>
      <td><%=h store.ext %></td>
      <td><%=h store.ward_id %></td>
      <td><%= link_to "Show", store %></td>
      <td><%= link_to "Edit", edit_store_path(store) %></td>
      <td><%= link_to "Destroy", store, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Store", new_store_path %></p>
