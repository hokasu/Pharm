
Todo:
2. Change input of new admission so that Departure date is set to Admission date once admission date is entered.


Wishlist:
Construct a route that looks like this:
# map.admit 'patients/:patient_id/admit', :controller => 'admissions', :action => 'admit'

In the distance:
set UI for iPhone

Done:
1. Add link in patients/show to add new admission for that patient_id
